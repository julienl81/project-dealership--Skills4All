{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.nbSeats) }}
    {{ form_row(form.nbDoors) }}
    {{ form_row(form.cost) }}
    {{ form_row(form.carCategory) }}


    <button class="btn btn-warning">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
